# ============================================================
# XIS AI/CV Assessment — Python dependencies
# Tested on Python 3.12. For GPU training (Colab), torch/torchvision
# are provided by the runtime; see docs/SETUP.md for the CUDA note.
# ============================================================

# --- Core CV & numerics ---
opencv-python==4.10.0.84
numpy==1.26.4
scipy==1.13.1
Pillow>=10.4.0
pillow-heif>=1.0.0        # decode iPhone HEIC originals (tools/heic_to_jpg.py)

# --- Deep learning (Mask R-CNN via torchvision) ---
torch==2.3.1
torchvision==0.18.1

# --- COCO dataset format (labels I/O + mAP eval) ---
pycocotools==2.0.7

# (Data augmentation — horizontal flip + brightness jitter — is implemented
#  directly in models/train.py with NumPy, so no extra dependency is needed.)

# --- Metrics, plotting, tables ---
matplotlib==3.9.0
pandas==2.2.2
scikit-learn==1.5.0
tqdm==4.66.4

# --- Notebook / reports ---
jupyter==1.0.0

# NOTE: MobileSAM (used ONLY for auto-labelling in dataset/sam_label.py — not the
# trained model) is installed separately to keep code-only clones light:
#     pip install timm git+https://github.com/ChaoningZhang/MobileSAM.git
# Checkpoint mobile_sam.pt (~37 MB) is hosted on Drive (see README) or fetched via
# huggingface_hub from "dhkim2810/MobileSAM". See docs/SETUP.md.
